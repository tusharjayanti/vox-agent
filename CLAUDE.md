# VoxAgent — Project Rules for Claude Code

> Read `DESIGN.md` in this directory before answering any substantive question. It is the source of truth for all architectural decisions.

## Project summary

VoxAgent is a text-based customer support agent with an inline evaluation layer. Every user turn is generated by Claude Sonnet and evaluated by Claude Haiku against a structured rubric, with retry and fallback logic based on the verdict. See `DESIGN.md` for full context.

## Non-negotiable conventions

These are decisions made during design. Do not question or relitigate them — if the user wants to change one, they will ask explicitly.

### Stack
- **Python 3.12+** with **uv** for dependency management. Never suggest `pip`, `poetry`, `pipenv`, or `requirements.txt`.
- **FastAPI** for the web layer. Never suggest Flask or Django.
- **asyncpg + raw SQL** for Postgres. **Do not introduce SQLAlchemy, SQLModel, or any ORM.**
- **Anthropic SDK** (`anthropic` Python package) as the only LLM client. Do not add OpenAI, Ollama, or other providers.
- **pydantic v2** for all request/response and internal data models. Do not use `dataclasses` where a Pydantic model makes sense.
- **pgvector** for vector storage in Phase 2. No external vector DB (Pinecone, Weaviate, Chroma, etc.).
- **Voyage AI** for embeddings in Phase 2. Not OpenAI embeddings.

### Models (swappable via .env, but defaults are fixed)
- Generator: `claude-sonnet-4-6`
- Judge: `claude-haiku-4-5-20251001`
- Model names come from config, never hardcoded in business logic.

### Code style
- `async`/`await` throughout. All I/O functions are async. No sync database calls, no sync HTTP calls.
- Type hints on every function signature. Use modern syntax (`list[str]`, `str | None`, not `List` / `Optional`).
- Import order: stdlib, third-party, local — enforced by ruff.
- Prefer small, single-purpose modules. `evaluator.py` contains only evaluation logic; `llm.py` contains only the LLM client wrapper; etc.
- No business logic in `main.py` — it wires routes to the agent and nothing else.
- Keep functions short. If a function exceeds ~40 lines, it probably needs splitting.

### Data layer
- Migrations are numbered `.sql` files in `migrations/` — never auto-generated.
- Raw SQL strings use `$1`, `$2` parameter placeholders (asyncpg style, not `%s`).
- Each query is its own function in `db.py` with a clear name (`insert_turn`, `fetch_conversation`, etc.). Do not inline SQL in agent logic.
- Use asyncpg's connection pool; obtain connections via `async with pool.acquire()`.

### Observability
- Every assistant turn writes to `turns` and `evaluations` tables — non-optional.
- Log latency in ms, input/output tokens from the Anthropic response, retry count.
- Use structured logging (stdlib `logging` with extra fields) — no `print()` in application code.

### Testing
- Tests go in `tests/`. One test file per source module being tested.
- Use `pytest-asyncio` with `asyncio_mode = "auto"`. No decorators on every test.
- The LLM client is mocked in unit tests. Only integration tests hit the real API, and they're marked with `@pytest.mark.integration` so they're skippable.

## What NOT to generate

- Dockerfiles, docker-compose.yml, CI configs, Kubernetes manifests — v1 scope is local-only. Mention deferred items in `DESIGN.md §11`.
- Auth, rate limiting, or user-management code. Not in v1.
- Streaming endpoints or WebSockets. Not in v1.
- Any voice/STT/TTS code.
- Redis. Use the in-memory dict.
- Frontend code beyond a minimal CLI if explicitly requested.

## Build phases

Do not skip ahead. The user drives phase progression explicitly.

- **Phase 1** (current): core eval loop, no RAG
  - 1.1 scaffold (done or in progress)
  - 1.2 FastAPI skeleton (`/chat` hardcoded + `/healthz`)
  - 1.3 Anthropic SDK wrapper + real Sonnet responses
  - 1.4 In-memory conversation store
  - 1.5 Heuristic evaluator
  - 1.6 LLM-as-judge (Haiku)
  - 1.7 Retry + fallback
  - 1.8 Postgres logging
  - 1.9 Tests
- **Phase 2**: RAG (corpus, pgvector, Voyage, retriever, grounding heuristic)
- **Phase 3**: demo polish (meta-eval, README, diagram, example traces)

Work one step at a time. After each step, stop and let the user review the diff and run the code before moving on.

## Interaction style

- **Explain before generating.** Before writing files for a new step, state what files will be created/modified and why. Wait for confirmation only if the plan deviates from `DESIGN.md`.
- **Small diffs.** Prefer multiple small edits over one large rewrite. The user is reviewing every change.
- **Call out assumptions.** If something in `DESIGN.md` is ambiguous for a specific line of code, surface the choice and the alternative.
- **No preamble bloat.** Skip "Great question!" and "I'd be happy to help you with that!" — go straight to the substance.

## The user

The user is Tushar — a software engineer transitioning to AI/ML. Explanations of Python-tooling and AI-SDK specifics are welcome (uv patterns, Anthropic SDK usage, asyncpg patterns). He is comfortable with GitHub workflows and repo-based development. He does not need JavaScript, frontend, or DevOps tutorials unless he asks.
